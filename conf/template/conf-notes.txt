Available images for OpenSTLinux layers are:

  - The system based on QT framework

      myir-image-full   -   MYiR HMI of image based on QT framework   (require 'openstlinux-eglfs' distro)

  - The system based on OpenSTLinux:

      myir-image-core   -   OpenSTLinux core image(A streamlined system that supports only basic functions)
      		and more images are available on meta-myir-st/recipes-myir/images.
